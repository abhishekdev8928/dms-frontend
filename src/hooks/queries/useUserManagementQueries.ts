import { useQuery } from '@tanstack/react-query';
// Import your API functions here as they are created
// Example structure for future user management queries

/**
 * Future: Hook to get all users
 * @access SUPER_ADMIN
 */
// export const useUsers = (params?: IGetAllUsersParams) => {
//   return useQuery({
//     queryKey: ['users', params],
//     queryFn: () => getAllUsers(params),
//     staleTime: 30000, // 30 seconds
//   });
// };

/**
 * Future: Hook to get a single user
 * @access SUPER_ADMIN
 */
// export const useUser = (userId: string) => {
//   return useQuery({
//     queryKey: ['user', userId],
//     queryFn: () => getUserById(userId),
//     enabled: !!userId,
//     staleTime: 30000,
//   });
// };

// This file is ready for future read operations related to user management
export {};